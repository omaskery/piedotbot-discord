name: Build and Test

on:
  pull_request:

jobs:
  test:
    uses: ./.github/workflows/_docker.yml
    with:
      registry: ${{ vars.REGISTRY }}
      image_name: ${{ vars.IMAGE_NAME }}
      push: false
